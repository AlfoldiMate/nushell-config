# env.nu — loaded before config.nu
#
# Kept empty on purpose. Since Nushell 0.101 everything, environment variables
# included, belongs in config.nu (here: conf/env.nu). This file only still
# exists because Nushell loads it for backwards compatibility.
#
# What Nushell itself sets before this file:  config env --default
